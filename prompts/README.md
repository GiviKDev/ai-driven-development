# Prompts

Copilot prompt file samples (`.prompt.md` files).
Each file defines a reusable prompt that can be
invoked by name in Copilot chat.

This folder is a catalog of reusable samples.

Currently empty. Samples will be added as they
emerge from real projects.
