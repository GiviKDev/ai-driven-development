---
applyTo: "**/*.md"
description: "Markdown formatting and writing rules"
---

# Markdown Rules

## Formatting

- Line length: soft-wrap at 60 characters for
  prose. Tables and code blocks may exceed this.
- Use `#` for H1 (document title, one per file).
  `##` for sections, `###` for subsections.
  No H4 or deeper. If you need H4, restructure
  the content.
- Use `-` for bullet lists. Use `1.` for ordered
  lists. Indent nested lists with two spaces.
- Use fenced code blocks with language identifiers.

## Punctuation

- No curly quotes. Straight quotes only.

## Links

- Use relative paths for internal links.
- Link to specific files, not folders.
- No bare URLs in prose. Wrap in link syntax.

## Structure

- No trailing whitespace.
- Files end with a single newline.
- Separate sections with a single blank line.
- One concept per file. Replace outdated content
  instead of appending updates.

## Writing style

- Plain language. No marketing copy, no filler.
- Be opinionated. Take a position, defend it.
- Brief paragraphs. If a paragraph exceeds 5
  lines, split it.
- No filler phrases: "it should be noted that",
  "it is important to", "in order to".
